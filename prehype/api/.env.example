# Activation API configuration
DATABASE_PUBLIC_URL=postgres://user:password@localhost:5432/lineage_war
PORT=3001
CORS_ORIGIN=http://localhost:5174

# Admin auth — set a strong secret, pass via X-Admin-Key header
ADMIN_SECRET=change-me-before-deploying
